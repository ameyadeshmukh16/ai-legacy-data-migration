# Architecture
SQLAlchemy profiles the source; LangChain maps semantics and generates rules/docs; LangGraph orchestrates seven stages and pauses for low-confidence human review; Snowflake receives run-specific staging data; validation and dbt block completion on failure; LangFuse and a hash-chained audit log provide observability and governance.
