# Great Expectations Checkpoints
Connect the project to three GX checkpoints:
1. source_baseline
2. post_extraction
3. post_load

Recommended expectations: row counts, required columns, primary-key uniqueness, null-rate tolerance, FK integrity, approved status-code domains and duplicate business-key checks. A failed validation must block completion.
